import React from 'react';
import ReactDOM from 'react-dom';
import './index.css'; // remove if not using custom CSS
import App from './App';

ReactDOM.render(<App />, document.getElementById('root'));
