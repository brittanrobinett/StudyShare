import React, { Component } from 'react';
//import { Link } from 'react-router-dom';

export default class Groups extends Component {
    render() {
        return (
            <h1>This is where you see groups</h1>
        );
    }
}